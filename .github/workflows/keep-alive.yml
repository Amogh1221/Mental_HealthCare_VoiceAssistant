name: Keep Hugging Face Space Alive

on:
  schedule:
    - cron: '0 */12 * * *' # Every 12 hours
  workflow_dispatch: # Allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: |
          curl -I https://huggingface.co/spaces/Amogh1221/Mental_HealthCare_VoiceAssistant
